import React from 'react'
import MainTemplate from '../common/MainTemplate'

export const DatabasePage: React.FC = () => {
    return (
        <MainTemplate>
            <h1>Database Page, the page that will show the pages</h1>
        </MainTemplate>
    )
}