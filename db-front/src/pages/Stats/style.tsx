import styled from 'styled-components';

export const Stats = styled.div`
  background: var(--primary-color);
  color: var(--foreground-color);
  border-radius: var(--border-radius);
  padding: var(--padding);
`;

export const StatsRow = styled.p`
  display: flex;
  gap: 10px;
  padding: 10px 0;
`;
